import React from 'react';
import CheckerBoard from './checker-board/checker-board';

export default function CheckersPage() {
    return (
        <div className="checker-board-page">
            <CheckerBoard/>
        </div>
    );
}